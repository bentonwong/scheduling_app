<h1>Teams</h1>

<div>
  <table>
    <tr>
      <th>ID</th>
      <th>Name</th>
    </tr>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.id %></td>
        <td><%= team.name %></td>
      <tr>
    <% end %>
  </table>
</div>
