<h1>Teams</h1>

<div>
  <table>
    <tr>
      <th>ID</th>
      <th>Name</th>
    </tr>
    <% @teams.sort_by(&:id).each do |team| %>
      <tr>
        <td><%= link_to team.id, team_path(team), method: :get %></td>
        <td><%= team.name %></td>
      <tr>
    <% end %>
  </table>
</div>
